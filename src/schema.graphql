type User {
  id: ID!
  firstName: String!
  lastName: String!
}

type Query {
  hello: String!
  helloWithName(name: String!): String!
  getUsers: [User]
  getUser(id: ID!): User
}

type Mutation {
  addUser(firstName: String!, lastName: String!): User
}